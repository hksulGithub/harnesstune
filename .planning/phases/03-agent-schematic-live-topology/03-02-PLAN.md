---
phase: 03-agent-schematic-live-topology
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/panels/SchematicPanel.ts
  - src/panels/index.ts
  - src/webview/schematic/index.tsx
  - src/webview/schematic/vscodeApi.ts
  - src/webview/schematic/App.tsx
  - src/webview/schematic/components/Toolbar.tsx
  - src/webview/schematic/components/GraphArea.tsx
  - src/webview/schematic/components/TopologyNode.tsx
  - src/webview/schematic/components/TopologyEdge.tsx
  - src/webview/schematic/components/Minimap.tsx
  - src/webview/schematic/components/WorkspaceSelector.tsx
  - src/webview/schematic/styles/schematic.css
  - esbuild.mjs
autonomous: true
requirements:
  - SCHM-01
  - SCHM-04
  - SCHM-05
  - SCHM-06

must_haves:
  truths:
    - "Schematic WebviewPanel opens in VSCode with SVG graph rendering"
    - "Graph uses hierarchical top-down layout via d3-hierarchy tree"
    - "Zoom in, zoom out, and fit-to-view all work"
    - "Graph can be panned by mouse drag"
    - "Active edges show traveling dot animation"
    - "Completed nodes show dashed border and reduced opacity"
    - "Minimap appears when graph has more than 5 nodes"
    - "Workspace selector dropdown filters graph by workspace"
    - "App.tsx handles schematic:nodeDetail response and passes session + events to AgentDetailPanel"
  artifacts:
    - path: "src/panels/SchematicPanel.ts"
      provides: "WebviewPanel host class for schematic"
      exports: ["SchematicPanel"]
    - path: "src/webview/schematic/App.tsx"
      provides: "Root React component for schematic webview"
      exports: ["App"]
    - path: "src/webview/schematic/components/GraphArea.tsx"
      provides: "SVG graph with zoom/pan and node/edge rendering"
      exports: ["GraphArea"]
    - path: "dist/webview/schematic.js"
      provides: "Built schematic webview bundle"
  key_links:
    - from: "src/panels/SchematicPanel.ts"
      to: "dist/webview/schematic.js"
      via: "webview HTML script src"
      pattern: "schematic\\.js"
    - from: "src/webview/schematic/App.tsx"
      to: "src/types/topology.ts"
      via: "TopologyState import for rendering"
      pattern: "import.*TopologyState"
    - from: "esbuild.mjs"
      to: "src/webview/schematic/index.tsx"
      via: "fourth webview entry point"
      pattern: "schematic/index\\.tsx"
    - from: "src/webview/schematic/App.tsx"
      to: "src/webview/shared/components/AgentDetailPanel.tsx"
      via: "schematic:nodeDetail message populates selectedSession and selectedEvents state"
      pattern: "schematic:nodeDetail"
---

<objective>
SchematicPanel host class and full React SVG graph UI: toolbar with zoom/pan/fit controls and workspace selector, SVG graph area with D3-positioned nodes and animated edges, minimap, and CSS styling with VSCode variables.

Purpose: Deliver the complete visual rendering layer for the agent topology graph, consuming TopologyState from Plan 01's data contracts.
Output: SchematicPanel.ts, complete React SVG component tree, schematic.css, fourth esbuild webview bundle.
</objective>

<execution_context>
@/Users/hksul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hksul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-schematic-live-topology/03-CONTEXT.md
@.planning/phases/03-agent-schematic-live-topology/03-UI-SPEC.md
@.planning/phases/03-agent-schematic-live-topology/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan renders -->

From src/types/topology.ts (created in Plan 01):
```typescript
export interface TopologyNode {
  sessionId: string;
  parentSessionId: string | null;
  workspaceId: string;
  agentRole: string | null;
  model: string | null;
  status: AgentControlState;  // 'running' | 'paused' | 'stopping' | 'stopped'
  opacity: number;            // 1.0 active, 0.5 completed
  x: number;
  y: number;
  startedAt: number;
  stoppedAt: number | null;
}

export interface TopologyEdge {
  id: string;
  sourceSessionId: string;
  targetSessionId: string;
  isActive: boolean;
  status: AgentControlState;
}

export interface TopologyState {
  nodes: TopologyNode[];
  edges: TopologyEdge[];
}
```

From src/types/messages.ts (extended in Plan 01):
```typescript
// Added in Plan 01:
| { type: 'schematic:topologyUpdate'; state: TopologyState }
| { type: 'schematic:nodeUpdate'; node: TopologyNode }
| { type: 'schematic:nodeDetail'; session: AgentSession | null; events: AgentEvent[] }
| { type: 'schematic:requestState'; workspaceId?: string }
| { type: 'schematic:selectNode'; sessionId: string }
```

From src/types/agent.ts:
```typescript
export interface AgentSession {
  sessionId: string;
  workspaceId: string;
  pid?: number;
  controlState: AgentControlState;
  pausedAt?: number;
  startedAt: number;
  model?: string;
  agentRole?: string;
}
```

From src/panels/DashboardPanel.ts (pattern to replicate):
```typescript
export class DashboardPanel {
  public static readonly viewType = 'harnesstune.dashboard';
  public static currentPanel: DashboardPanel | undefined;
  public static createOrShow(extensionUri: vscode.Uri): DashboardPanel;
  public static revive(panel: vscode.WebviewPanel, extensionUri: vscode.Uri): void;
  public postMessage(message: HostToWebviewMessage): void;
  public readonly onDidReceiveMessage: vscode.Event<WebviewToHostMessage>;
}
```

From esbuild.mjs (current entry points):
```javascript
// extensionConfig: src/extension.ts -> dist/extension.js (CJS, node)
// sidebarConfig: src/webview/sidebar/index.tsx -> dist/webview/sidebar.js (ESM, browser)
// dashboardConfig: src/webview/dashboard/index.tsx -> dist/webview/dashboard.js (ESM, browser)
// Need to add: schematicConfig: src/webview/schematic/index.tsx -> dist/webview/schematic.js (ESM, browser)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SchematicPanel host class, esbuild config, vscodeApi, and React entry point</name>
  <files>src/panels/SchematicPanel.ts, src/panels/index.ts, src/webview/schematic/vscodeApi.ts, src/webview/schematic/index.tsx, esbuild.mjs</files>
  <read_first>
    - src/panels/DashboardPanel.ts
    - src/panels/index.ts
    - src/webview/dashboard/vscodeApi.ts
    - src/webview/dashboard/index.tsx
    - esbuild.mjs
    - .planning/phases/03-agent-schematic-live-topology/03-CONTEXT.md (D-16, D-17, D-20, D-22)
  </read_first>
  <action>
    1. Create `src/panels/SchematicPanel.ts` following the exact `DashboardPanel.ts` pattern:
       - `public static readonly viewType = 'harnesstune.schematic'`
       - `public static currentPanel: SchematicPanel | undefined` (public static, same pattern as DashboardPanel)
       - `createOrShow(extensionUri: vscode.Uri): SchematicPanel` — creates `WebviewPanel` with title `'HarnessTune Schematic'`, `ViewColumn.Two` (opens beside dashboard), `retainContextWhenHidden: false` (D-17), `enableScripts: true`, `localResourceRoots: [Uri.joinPath(extensionUri, 'dist')]`
       - `revive(panel, extensionUri)` — for WebviewPanelSerializer
       - `postMessage(message: HostToWebviewMessage)` — typed message push
       - `onDidReceiveMessage` EventEmitter for `WebviewToHostMessage`
       - `getHtmlForWebview()` — nonce-based CSP identical to DashboardPanel pattern, loads `dist/webview/schematic.js` and `dist/webview/schematic.css`
       - On dispose: `SchematicPanel.currentPanel = undefined`

    2. Update `src/panels/index.ts`:
       - Add `export { SchematicPanel } from './SchematicPanel';`

    3. Create `src/webview/schematic/vscodeApi.ts`:
       - Identical pattern to `src/webview/dashboard/vscodeApi.ts`
       - `acquireVsCodeApi()` called once in module scope
       - Export typed `VsCodeApi` with `postMessage(WebviewToHostMessage)`, `getState()`, `setState()`

    4. Create `src/webview/schematic/index.tsx`:
       - React 18 `createRoot(document.getElementById('root')!)` entry point
       - Import `App` from `./App` and `./styles/schematic.css`
       - Render `<App />`

    5. Add schematic entry point to `esbuild.mjs`:
       - Add `schematicConfig` object identical to `dashboardConfig` but with:
         - `entryPoints: ['src/webview/schematic/index.tsx']`
         - `outfile: 'dist/webview/schematic.js'`
       - Add to both `isWatch` and build `Promise.all` arrays
       - In watch mode: `const schematicCtx = await esbuild.context(schematicConfig)` and add to `Promise.all`
       - In build mode: add `esbuild.build(schematicConfig)` to `Promise.all`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.extension.json && npx tsc --noEmit -p tsconfig.webview.json && node esbuild.mjs && ls dist/webview/schematic.js</automated>
  </verify>
  <acceptance_criteria>
    - src/panels/SchematicPanel.ts contains `public static readonly viewType = 'harnesstune.schematic'`
    - src/panels/SchematicPanel.ts contains `public static currentPanel: SchematicPanel | undefined`
    - src/panels/SchematicPanel.ts contains `retainContextWhenHidden: false`
    - src/panels/SchematicPanel.ts contains `schematic.js`
    - src/panels/SchematicPanel.ts contains `schematic.css`
    - src/panels/SchematicPanel.ts contains `ViewColumn.Two`
    - src/panels/index.ts contains `SchematicPanel`
    - src/webview/schematic/vscodeApi.ts contains `acquireVsCodeApi`
    - src/webview/schematic/index.tsx contains `createRoot`
    - esbuild.mjs contains `schematic/index.tsx`
    - esbuild.mjs contains `dist/webview/schematic.js`
    - dist/webview/schematic.js exists after build
    - npx tsc --noEmit -p tsconfig.extension.json exits 0
    - npx tsc --noEmit -p tsconfig.webview.json exits 0
  </acceptance_criteria>
  <done>SchematicPanel host class created following DashboardPanel pattern. Fourth esbuild entry point configured and builds to dist/webview/schematic.js. vscodeApi singleton and React entry point created. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: React SVG graph components, toolbar, minimap, and schematic CSS</name>
  <files>src/webview/schematic/App.tsx, src/webview/schematic/components/Toolbar.tsx, src/webview/schematic/components/GraphArea.tsx, src/webview/schematic/components/TopologyNodeComponent.tsx, src/webview/schematic/components/TopologyEdgeComponent.tsx, src/webview/schematic/components/Minimap.tsx, src/webview/schematic/components/WorkspaceSelector.tsx, src/webview/schematic/styles/schematic.css</files>
  <read_first>
    - .planning/phases/03-agent-schematic-live-topology/03-UI-SPEC.md (ALL sections — this is the visual contract)
    - .planning/phases/03-agent-schematic-live-topology/03-CONTEXT.md (D-01 through D-15, D-19)
    - src/webview/schematic/vscodeApi.ts (created in Task 1)
    - src/webview/schematic/index.tsx (created in Task 1)
    - src/types/topology.ts (created in Plan 01)
    - src/types/messages.ts (extended in Plan 01)
    - src/webview/dashboard/styles/dashboard.css (Phase 2 CSS — match conventions)
    - src/webview/shared/components/AgentDetailPanel.tsx (shared detail panel from Plan 01)
  </read_first>
  <action>
    1. Create `src/webview/schematic/App.tsx` — Root schematic component:
       - State: `topologyState: TopologyState` (from `vscode.getState()` or default `{ nodes: [], edges: [] }`)
       - State: `selectedNodeId: string | null`
       - State: `selectedSession: AgentSession | null` (populated by schematic:nodeDetail response)
       - State: `selectedEvents: AgentEvent[]` (populated by schematic:nodeDetail response)
       - State: `viewTransform: { x: number, y: number, scale: number }` (default `{ x: 0, y: 0, scale: 1.0 }`)
       - State: `workspaceFilter: string | null` (null = "All workspaces")
       - State: `workspaces: { id: string, name: string }[]` (from workspaces:update messages)
       - `useEffect` message listener handling these message types:
         - `schematic:topologyUpdate` -> set topologyState
         - `schematic:nodeUpdate` -> update single node in topologyState
         - `schematic:nodeDetail` -> set selectedSession from msg.session, set selectedEvents from msg.events (this is the response to schematic:selectNode, carrying the clicked node's session and event history for AgentDetailPanel)
         - `workspaces:update` -> set workspaces list
       - When user clicks a node (selectedNodeId changes): `vscode.postMessage({ type: 'schematic:selectNode', sessionId: selectedNodeId })` — the extension host responds with `schematic:nodeDetail`
       - On mount: `vscode.postMessage({ type: 'schematic:requestState' })`
       - Persist to `vscode.setState()` on state changes: `{ workspaceFilter, selectedNodeId, viewTransform }`
       - Restore from `vscode.getState()` on mount (D-17)
       - Layout: `<div className="schematic-root">` containing `<Toolbar>`, body row with `<GraphArea>` + `<AgentDetailPanel>`
       - Pass `showControls={false}` to `AgentDetailPanel` (D-18 — schematic is read-only in Phase 3)
       - Pass `session={selectedSession}` and `events={selectedEvents}` to `AgentDetailPanel`
       - Only render `AgentDetailPanel` when `selectedNodeId !== null`

    2. Create `src/webview/schematic/components/Toolbar.tsx`:
       - Left group: "Fit to View" button, zoom in (`+`), zoom level readout (`{Math.round(scale * 100)}%`), zoom out
       - Right group: `<WorkspaceSelector>` component
       - Button spec per UI-SPEC: `min-width: 28px; min-height: 28px; padding: 4px 8px; border-radius: 3px; background: transparent; border: none`
       - Hover: `var(--vscode-list-hoverBackground)`. Active: `var(--vscode-toolbar-activeBackground)`
       - Focus: `outline: 1px solid var(--vscode-focusBorder); outline-offset: -1px`
       - Toolbar: `role="toolbar" aria-label="Graph controls"`, height 36px
       - Tooltips per UI-SPEC copywriting: "Fit graph to view (F)", "Zoom in (+)", "Zoom out (-)"
       - Props: `{ scale, onFitToView, onZoomIn, onZoomOut, workspaceFilter, workspaces, onWorkspaceChange }`

    3. Create `src/webview/schematic/components/WorkspaceSelector.tsx`:
       - HTML `<select>` element, 160px max-width
       - First option: "All workspaces" (value `""`)
       - Per-workspace options: `{workspaceName}` truncated to 20 chars with `...`
       - Styled via `var(--vscode-dropdown-*)` variables
       - `aria-label="Filter by workspace"`

    4. Create `src/webview/schematic/components/GraphArea.tsx`:
       - SVG root: `<svg width="100%" height="100%" role="application" aria-label="Agent topology graph">`
       - Viewport `<g>` with `transform={translate(${x}, ${y}) scale(${scale})}`
       - Render edges first (below nodes), then nodes (on top)
       - Zoom: `onWheel` handler — `deltaY > 0` zooms out (scale / 1.1), `deltaY < 0` zooms in (scale * 1.1). Range: 0.2 to 3.0.
       - Pan: `onMouseDown` sets dragging, `onMouseMove` updates `{ x, y }` by delta, `onMouseUp` clears. Cursor: `grab` default, `grabbing` while dragging.
       - Keyboard: `+`/`=` zoom in, `-` zoom out, `0` reset to 100%, `F` fit to view, `Escape` deselect
       - Fit-to-view algorithm: compute bounding box of all nodes (min/max x, y, accounting for node width 140 and height 44), calculate scale to fit within SVG dimensions with 24px padding (xl token), center the viewport.
       - Click on SVG background (not on a node): deselect current node
       - Empty state: when `nodes.length === 0`, render centered text per UI-SPEC copywriting: heading "No agents running", body "Start a Claude Code session to see the agent hierarchy appear here."
       - Reduced motion: check `window.matchMedia('(prefers-reduced-motion: reduce)').matches` and pass to edge components
       - Renders `<Minimap>` when `nodes.length > 5`

    5. Create `src/webview/schematic/components/TopologyNodeComponent.tsx`:
       - SVG `<g>` group per UI-SPEC node rendering spec:
         - `<rect width="140" height="44" rx="6">` with `fill="var(--vscode-editor-background)"`, `stroke={statusColor}`, `stroke-width="1.5"`, `stroke-dasharray={node.status === 'stopped' ? '4 3' : 'none'}`
         - Status dot: `<circle cx="16" cy="22" r="4" fill={statusColor}>`
         - Primary label (agent name): `<text x="28" y="18" font-size="13" font-weight="600">` truncated to 18 chars
         - Secondary label (session ID): `<text x="28" y="34" font-size="11" fill="var(--vscode-descriptionForeground)">sess:{sessionId.slice(0,8)}</text>`
         - Status chip: `<rect class="status-chip" x="88" y="28" width="44" height="12" rx="6">` + centered text
         - Error state: additional outer `<rect>` at `x="-3" y="-3" width="146" height="50" rx="8"` with error stroke
       - `tabIndex={0}`, `role="button"`, `aria-label="{agentName}, {status}, session {sessionId.slice(0,8)}, click to inspect"`
       - Selected state: `fill="var(--vscode-list-activeSelectionBackground)"`, text `var(--vscode-list-activeSelectionForeground)`
       - Hover: `fill="var(--vscode-list-hoverBackground)"` (managed via CSS class `.node:hover rect`)
       - Focus ring: additional `<rect>` with `stroke="var(--vscode-focusBorder)"` stroke-width="2" when `:focus-visible`
       - `onClick` -> select node. `onKeyDown` Enter/Space -> select. Arrow keys -> navigate tree.
       - Apply `opacity={node.opacity}` to the `<g>` wrapper
       - Status color mapping:
         - running: `var(--vscode-terminal-ansiGreen)`
         - paused: `var(--vscode-terminal-ansiYellow)`
         - stopped: `var(--vscode-descriptionForeground)`
         - error: `var(--vscode-errorForeground)`
       - Status chip text: running="RUNNING", paused="PAUSED", stopped="DONE", error="ERROR"

    6. Create `src/webview/schematic/components/TopologyEdgeComponent.tsx`:
       - `<path>` with cubic bezier from source node bottom-center to target node top-center
       - Control points: offset 60px vertically (halfway between source and target)
       - Source point: `(sourceNode.x + 70, sourceNode.y + 44)` (bottom center of 140px wide node)
       - Target point: `(targetNode.x + 70, targetNode.y)` (top center)
       - Path: `M ${sx} ${sy} C ${sx} ${sy+60}, ${tx} ${ty-60}, ${tx} ${ty}`
       - `stroke-width="1.5"`, `fill="none"`, stroke color per edge status (same mapping as node)
       - Dashed stroke for completed: `stroke-dasharray="4 3"`
       - Edge ID path: `id="edge-${edge.id}"` for animateMotion reference
       - Traveling dot animation (when `edge.isActive && !reducedMotion`):
         ```xml
         <circle r="3" fill={edgeColor}>
           <animateMotion dur="1.5s" repeatCount="indefinite">
             <mpath xlinkHref={`#edge-${edge.id}`} />
           </animateMotion>
         </circle>
         ```
       - When `edge.status === 'paused'`: static dot at path midpoint (frozen, no animation)
       - When `!edge.isActive` or `edge.status === 'stopped'`: no dot
       - When `reducedMotion === true` and `edge.isActive`: static dot at midpoint instead of animation
       - `role="presentation" aria-hidden="true"`

    7. Create `src/webview/schematic/components/Minimap.tsx`:
       - `position: absolute; bottom: 12px; right: 12px; width: 120px; height: 80px`
       - Background: `var(--vscode-sideBar-background)`, border: `1px solid var(--vscode-widget-border)`, border-radius: 4px
       - Renders scaled-down `<rect>` per node (4px x 3px), color = status color
       - Viewport indicator: semi-transparent `<rect>` showing current visible area relative to full graph bounds
       - `role="img" aria-label="Graph minimap, showing {N} agents"`
       - Hidden when `nodes.length <= 5`
       - Not interactive (display only)

    8. Create `src/webview/schematic/styles/schematic.css`:
       - Root: `.schematic-root { display: flex; flex-direction: column; height: 100vh; background: var(--vscode-editor-background); font-family: var(--vscode-font-family); }`
       - Toolbar: `.toolbar { flex-shrink: 0; height: 36px; display: flex; align-items: center; padding: 0 12px; background: var(--vscode-sideBar-background); border-bottom: 1px solid var(--vscode-widget-border); }`
       - Toolbar left/right: `.toolbar-left { display: flex; align-items: center; gap: 4px; }`, `.toolbar-right { margin-left: auto; }`
       - Toolbar button: `.toolbar-btn { background: transparent; border: none; min-width: 28px; min-height: 28px; padding: 4px 8px; border-radius: 3px; font-size: 13px; color: var(--vscode-foreground); cursor: pointer; }`, hover/active/focus states
       - Body: `.schematic-body { display: flex; flex: 1; overflow: hidden; }`
       - Graph area: `.graph-area { flex: 1; position: relative; overflow: hidden; }`
       - Detail panel: `.schematic-detail { width: 300px; min-width: 300px; flex-shrink: 0; border-left: 1px solid var(--vscode-widget-border); overflow-y: auto; background: var(--vscode-sideBar-background); }`
       - SVG zoom cursors: `.graph-svg { cursor: grab; }`, `.graph-svg.dragging { cursor: grabbing; }`
       - Node hover: `.node:hover > rect:first-child { fill: var(--vscode-list-hoverBackground); }`
       - Node selected: `.node.selected > rect:first-child { fill: var(--vscode-list-activeSelectionBackground); }`
       - Minimap: `.minimap { position: absolute; bottom: 12px; right: 12px; width: 120px; height: 80px; ... }`
       - Empty state: `.empty-state { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100%; padding: 32px; text-align: center; }`
       - Workspace selector: `.workspace-select { background: var(--vscode-dropdown-background); color: var(--vscode-dropdown-foreground); border: 1px solid var(--vscode-dropdown-border); border-radius: 3px; padding: 2px 4px; font-size: 13px; max-width: 160px; }`
       - Zoom readout: `.zoom-readout { font-size: 11px; color: var(--vscode-descriptionForeground); min-width: 40px; text-align: center; }`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.webview.json && npx tsc --noEmit -p tsconfig.extension.json && node esbuild.mjs && ls dist/webview/schematic.js dist/webview/schematic.css</automated>
  </verify>
  <acceptance_criteria>
    - src/webview/schematic/App.tsx contains `schematic:topologyUpdate` and `schematic:requestState`
    - src/webview/schematic/App.tsx contains `schematic:nodeDetail` handler that sets selectedSession and selectedEvents
    - src/webview/schematic/App.tsx contains `schematic:selectNode` postMessage when a node is clicked
    - src/webview/schematic/App.tsx contains `showControls={false}`
    - src/webview/schematic/App.tsx contains `session={selectedSession}` and `events={selectedEvents}` passed to AgentDetailPanel
    - src/webview/schematic/App.tsx contains `vscode.getState` and `vscode.setState`
    - src/webview/schematic/components/Toolbar.tsx contains `role="toolbar"` and `aria-label="Graph controls"`
    - src/webview/schematic/components/Toolbar.tsx contains `Fit` and `+` and zoom readout
    - src/webview/schematic/components/GraphArea.tsx contains `role="application"` and `aria-label="Agent topology graph"`
    - src/webview/schematic/components/GraphArea.tsx contains `onWheel` and `onMouseDown` and `onMouseMove`
    - src/webview/schematic/components/GraphArea.tsx contains `prefers-reduced-motion`
    - src/webview/schematic/components/TopologyNodeComponent.tsx contains `width="140"` and `height="44"` and `rx="6"`
    - src/webview/schematic/components/TopologyNodeComponent.tsx contains `tabIndex` and `role="button"`
    - src/webview/schematic/components/TopologyNodeComponent.tsx contains `RUNNING` and `PAUSED` and `DONE` and `ERROR`
    - src/webview/schematic/components/TopologyEdgeComponent.tsx contains `animateMotion` and `dur="1.5s"`
    - src/webview/schematic/components/TopologyEdgeComponent.tsx contains `role="presentation"` and `aria-hidden="true"`
    - src/webview/schematic/components/Minimap.tsx contains `role="img"` and `aria-label`
    - src/webview/schematic/components/WorkspaceSelector.tsx contains `All workspaces`
    - src/webview/schematic/styles/schematic.css contains `--vscode-editor-background` and `--vscode-sideBar-background`
    - src/webview/schematic/styles/schematic.css contains `.schematic-root` and `.toolbar` and `.graph-area` and `.schematic-detail`
    - esbuild.mjs contains `schematic/index.tsx` and `dist/webview/schematic.js`
    - dist/webview/schematic.js exists after build
    - dist/webview/schematic.css exists after build
    - npx tsc --noEmit -p tsconfig.webview.json exits 0
    - npx tsc --noEmit -p tsconfig.extension.json exits 0
  </acceptance_criteria>
  <done>Complete schematic WebviewPanel with React SVG graph rendering. SchematicPanel host class with nonce-CSP. Full component tree: Toolbar (zoom/pan/fit/workspace selector), GraphArea (SVG with zoom/pan/keyboard), TopologyNodeComponent (140x44 nodes with status colors/patterns), TopologyEdgeComponent (cubic bezier with traveling dot animation), Minimap (scaled replica), WorkspaceSelector (dropdown filter). All styled with VSCode CSS variables. Fourth esbuild bundle builds to dist/webview/schematic.js. App.tsx handles schematic:nodeDetail response to populate AgentDetailPanel with session and events after node click. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.extension.json` — extension host compiles clean
- `npx tsc --noEmit -p tsconfig.webview.json` — webview compiles clean
- `node esbuild.mjs` — all 4 bundles build (extension + sidebar + dashboard + schematic)
- `ls dist/webview/schematic.js dist/webview/schematic.css` — schematic bundle and CSS exist
- `grep "harnesstune.schematic" src/panels/SchematicPanel.ts` — viewType correct
- `grep "retainContextWhenHidden: false" src/panels/SchematicPanel.ts` — no retainContext
- `grep "animateMotion" src/webview/schematic/components/TopologyEdgeComponent.tsx` — edge animation present
- `grep "prefers-reduced-motion" src/webview/schematic/components/GraphArea.tsx` — accessibility check
- `grep "schematic:nodeDetail" src/webview/schematic/App.tsx` — node detail handler present
</verification>

<success_criteria>
SchematicPanel opens as a WebviewPanel with SVG-based topology graph. Nodes render as 140x44px rectangles with status colors, accessibility labels, and keyboard navigation. Edges render as cubic bezier paths with traveling dot animation on active edges. Toolbar provides zoom/pan/fit controls and workspace filtering. Minimap shows scaled replica for large graphs. All styling uses VSCode CSS variables. App.tsx handles schematic:nodeDetail response message and passes session + events to AgentDetailPanel for click-to-inspect (SCHM-03). Fourth esbuild bundle builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-schematic-live-topology/03-02-SUMMARY.md`
</output>
