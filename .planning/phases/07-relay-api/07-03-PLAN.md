---
phase: 07-relay-api
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - packages/harnesstune-relay/api/[[...route]].ts
  - packages/harnesstune-relay/vercel.json
  - packages/harnesstune-relay/.env.example
autonomous: false
requirements: [RLAY-01, RLAY-13]
user_setup:
  - service: turso
    why: "SQLite database persistence for relay data"
    env_vars:
      - name: TURSO_DATABASE_URL
        source: "Turso CLI: `turso db show <db-name> --url`"
      - name: TURSO_AUTH_TOKEN
        source: "Turso CLI: `turso db tokens create <db-name>`"
  - service: vercel
    why: "Serverless deployment for relay API"
    env_vars: []
    dashboard_config:
      - task: "Add TURSO_DATABASE_URL and TURSO_AUTH_TOKEN as environment variables"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "vercel deploy from packages/harnesstune-relay produces a live deployment"
    - "Health endpoint returns 200 on deployed relay"
    - "Schema push creates all tables in Turso"
  artifacts:
    - path: "packages/harnesstune-relay/api/[[...route]].ts"
      provides: "Vercel serverless entry point"
      min_lines: 3
    - path: "packages/harnesstune-relay/vercel.json"
      provides: "Vercel routing and build config"
      contains: "rewrites"
    - path: "packages/harnesstune-relay/.env.example"
      provides: "Documented env var requirements"
      contains: "TURSO_DATABASE_URL"
  key_links:
    - from: "api/[[...route]].ts"
      to: "src/app.ts"
      via: "handle(req) export"
      pattern: "app\\.fetch|handle"
---

<objective>
Create the Vercel deployment entry point, push the Drizzle schema to Turso, and verify the relay is live with a deployment checkpoint.

Purpose: This is the final plan that makes the relay a live, deployed service. Plans 01-02 built the code; this plan deploys it.

Output: Live Vercel deployment with all endpoints functional.
</objective>

<execution_context>
@/Users/hksul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hksul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-relay-api/07-CONTEXT.md
@.planning/phases/07-relay-api/07-01-SUMMARY.md
@.planning/phases/07-relay-api/07-02-SUMMARY.md

<interfaces>
From src/app.ts (Plan 01+02):
```typescript
export { app } from './app.js';
// Hono app with all routes and middleware
```

From src/index.ts:
```typescript
export const RELAY_VERSION = '0.1.0';
export { app } from './app.js';
```
</interfaces>
</context>

<threat_model>
## Threats and Mitigations

1. **Env var exposure in .env file committed to git** (HIGH) — Turso credentials leaked in version control.
   Mitigation: Only `.env.example` with placeholder values committed. `.env` added to .gitignore. Vercel env vars set via dashboard.

2. **Unauthenticated schema push** (LOW) — Schema push requires Turso credentials.
   Mitigation: Schema push runs locally with user's own credentials, not in CI. This is a one-time setup step.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Vercel entry point + config + schema push</name>
  <read_first>
    - packages/harnesstune-relay/src/app.ts
    - packages/harnesstune-relay/src/index.ts
    - packages/harnesstune-relay/package.json
    - packages/harnesstune-relay/tsconfig.json
    - packages/harnesstune-relay/drizzle.config.ts
    - .planning/phases/07-relay-api/07-CONTEXT.md
  </read_first>
  <files>
    packages/harnesstune-relay/api/[[...route]].ts
    packages/harnesstune-relay/vercel.json
    packages/harnesstune-relay/.env.example
  </files>
  <action>
1. Create `packages/harnesstune-relay/api/[[...route]].ts`:
```typescript
import { handle } from 'hono/vercel';
import { app } from '../src/app.js';

export default handle(app);
```

Note: The Hono Vercel adapter uses `handle()` from `hono/vercel` which wraps the Hono app for Vercel's serverless function format. This is a single catch-all route per CONTEXT.md D-01.

2. Create `packages/harnesstune-relay/vercel.json`:
```json
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/health", "destination": "/api" }
  ],
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build"
}
```

3. Create `packages/harnesstune-relay/.env.example`:
```
# Turso database credentials
# Get these from: turso db show <db-name> --url / turso db tokens create <db-name>
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# Relay config
RELAY_MIN_AGENT_VERSION=0.1.0
```

4. Add `.env` to the relay's `.gitignore` (create if needed):
```
.env
node_modules/
dist/
.vercel/
```

5. Run schema push to create tables in Turso. This requires the user's Turso env vars.
   If TURSO_DATABASE_URL and TURSO_AUTH_TOKEN are not set, inform the user they need to:
   a. Install Turso CLI: `curl -sSfL https://get.tur.so | bash`
   b. Create a database: `turso db create harnesstune-relay`
   c. Get URL: `turso db show harnesstune-relay --url`
   d. Get token: `turso db tokens create harnesstune-relay`
   e. Set env vars in a `.env` file in `packages/harnesstune-relay/`
   f. Run: `cd packages/harnesstune-relay && npx drizzle-kit push`

6. Verify TypeScript compilation: `cd packages/harnesstune-relay && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/hksul/Dropbox/Research_obsidian/ClaudeVault2/agents/Ethan/harnesstune/packages/harnesstune-relay && test -f api/\\[\\[...route\\]\\].ts && test -f vercel.json && test -f .env.example && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/harnesstune-relay/api/[[...route]].ts contains `handle(app)`
    - packages/harnesstune-relay/api/[[...route]].ts contains `hono/vercel`
    - packages/harnesstune-relay/vercel.json contains `"rewrites"`
    - packages/harnesstune-relay/vercel.json contains `"/api/(.*)"` or equivalent rewrite
    - packages/harnesstune-relay/vercel.json contains `"/health"`
    - packages/harnesstune-relay/.env.example contains `TURSO_DATABASE_URL`
    - packages/harnesstune-relay/.env.example contains `TURSO_AUTH_TOKEN`
    - packages/harnesstune-relay/.gitignore contains `.env`
  </acceptance_criteria>
  <done>Vercel catch-all entry point, vercel.json config, .env.example, and .gitignore all created. Schema push instructions provided to user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Vercel and verify relay is live</name>
  <files>none</files>
  <action>
Human deploys the relay to Vercel and verifies all endpoints work on the live deployment. Claude automates nothing in this task — deployment requires Vercel auth and Turso credentials.
  </action>
  <what-built>Complete relay API with Hono, Turso persistence, token auth, rate limiting, all CRUD endpoints, and Vercel deployment config.</what-built>
  <how-to-verify>
    1. Set up Turso database (if not done):
       - `curl -sSfL https://get.tur.so | bash`
       - `turso db create harnesstune-relay`
       - `turso db show harnesstune-relay --url` (copy URL)
       - `turso db tokens create harnesstune-relay` (copy token)

    2. Create `.env` in `packages/harnesstune-relay/` with TURSO_DATABASE_URL and TURSO_AUTH_TOKEN

    3. Push schema to Turso:
       - `cd packages/harnesstune-relay && npx drizzle-kit push`
       - Verify 5 tables created (channels, tokens, reports, messages, rate_limits)

    4. Deploy to Vercel:
       - `cd packages/harnesstune-relay && vercel deploy`
       - Add TURSO_DATABASE_URL and TURSO_AUTH_TOKEN as env vars in Vercel dashboard
       - Redeploy if env vars added after first deploy: `vercel deploy`

    5. Test health endpoint:
       - `curl https://YOUR-DEPLOYMENT.vercel.app/health`
       - Expected: `{"status":"ok","version":"0.1.0"}`

    6. Test registration:
       - `curl -X POST https://YOUR-DEPLOYMENT.vercel.app/api/channels -H "Content-Type: application/json" -d '{"name":"test-agent"}'`
       - Expected: 201 with `channelId` and `token`

    7. Test authenticated endpoint (using token from step 6):
       - `curl -H "Authorization: Bearer TOKEN_FROM_STEP_6" https://YOUR-DEPLOYMENT.vercel.app/api/channels/CHANNEL_ID`
       - Expected: 200 with channel metadata

    8. Test auth rejection:
       - `curl -H "Authorization: Bearer invalid-token" https://YOUR-DEPLOYMENT.vercel.app/api/channels/any-id`
       - Expected: 401

    9. Verify Vercel function logs do NOT contain raw token (check Vercel dashboard -> Deployments -> Functions -> Logs)
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human verification required"</automated>
  </verify>
  <done>Relay is live on Vercel. Health returns 200. Registration + auth + report round-trip all work. No raw tokens in logs.</done>
  <resume-signal>Type "approved" with the deployed URL, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- Vercel entry point exists and uses hono/vercel handle()
- vercel.json rewrites all /api/* and /health to the catch-all
- .env.example documents all required environment variables
- .env is in .gitignore
- Schema push creates all 5 tables in Turso
- Deployed relay responds to /health with 200
</verification>

<success_criteria>
- `vercel deploy` from packages/harnesstune-relay succeeds
- `curl /health` returns 200 with version
- Full registration + report upload round-trip works on live deployment
- Invalid tokens return 401
- No raw tokens in Vercel function logs
</success_criteria>

<output>
After completion, create `.planning/phases/07-relay-api/07-03-SUMMARY.md`
</output>
